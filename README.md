# Secure Review Frontend

Frontend приложение для системы безопасного ревью кода (Secure Review). Разработано с использованием Vue 3, TypeScript и
Vite.

## 🚀 Функциональность

- **Аутентификация**: Регистрация и вход пользователей (Email/Password).
- **GitHub Интеграция**:
    - Вход через GitHub (OAuth).
    - Привязка/Отвязка GitHub аккаунта в профиле пользователя.
    - Просмотр статуса подключения.
- **Управление профилем**:
    - Просмотр информации профиля.
    - Изменение пароля.
    - Обновление данных профиля.
- **Логирование**: Централизованная система сбора логов (Debug/Info/Warn/Error).

## 🛠 Технологический стек

- **Framework**: [Vue 3](https://vuejs.org/) (Composition API, `<script setup>`)
- **State Management**: [Pinia](https://pinia.vuejs.org/)
- **Routing**: [Vue Router](https://router.vuejs.org/)
- **Build Tool**: [Vite](https://vitejs.dev/)
- **Language**: [TypeScript](https://www.typescriptlang.org/)
- **HTTP Client**: [Axios](https://axios-http.com/)

## ⚙️ Предварительные требования

- Node.js (версия 18.0.0 или выше)
- npm (или yarn/pnpm)

## 📦 Установка и запуск

1. **Клонируйте репозиторий:**

   ```bash
   git clone https://github.com/IPodymov/secure-revie-front.git
   cd secure-revie-front
   ```

2. **Установите зависимости:**

   ```bash
   npm install
   ```

3. **Настройка окружения:**

   Создайте файл `.env.local` в корне проекта (или скопируйте пример, если есть) и укажите URL бэкенда:

   ```env
   # URL API вашего бэкенд сервера
   VITE_API_URL=http://localhost:8080/api/v1
   ```

4. **Запуск в режиме разработки:**
   ```bash
   npm run dev
   ```
   Приложение будет доступно по адресу: http://localhost:5173

## 🏗 Сборка для продакшена

Для сборки оптимизированной версии приложения выполните:

```bash
npm run build
```

Результат сборки будет в папке `dist`.

## � Документация

- [📂 Структура проекта](docs/STRUCTURE.md)
- [🔐 Настройка GitHub OAuth](docs/GITHUB_AUTH.md)
- [📝 Логирование](docs/LOGGING.md)В режиме разработки (dev) выводятся все логи, в продакшене — только Warn и Error.

Использование:

```typescript
import logger from '@/utils/logger';

logger.info('Action performed', {data: 123});
logger.error('Something went wrong', error);
```
